# Copy to .env.local — never commit real keys.

# Google Maps JavaScript API (dispatch / parent web maps)
# Restrict in GCP: HTTP referrers (your domain only) + Maps JavaScript API
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
